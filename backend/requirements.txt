# 后端依赖（骨架阶段：核心依赖初稿，版本号与最终清单待编码时确定）
# 规划技术选型参考：../初步设计方案.txt

# Web
fastapi
uvicorn[standard]
python-multipart        # 文件上传

# 数据库 / ORM
sqlalchemy
pymysql
cryptography            # MySQL 8 认证插件需要

# 向量库 / Embedding
chromadb
jieba                   # 中文分词，BM25 关键词召回（001 混合检索；打分自研 Okapi BM25）
# 可选：Reranker 精排（001），安装见 docs/重难点总结.md 第五部分 §5 镜像教程
# sentence-transformers
# torch

# 配置
python-dotenv

# 文档解析
pypdf                   # pdf 解析（可选）
python-docx

# 鉴权
pyjwt

# LLM 调用（OpenAI 兼容协议，可对接 Ollama / 通义千问 / Groq）
openai

# 异步任务（文档量大时启用）
# celery
# redis
