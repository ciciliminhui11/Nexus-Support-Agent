# 后端依赖（Python 3.14 实测兼容清单，2026-08-30）
# 规划技术选型参考：../初步设计方案.txt

# Web
fastapi
uvicorn[standard]
python-multipart        # 文件上传

# 数据库 / ORM
sqlalchemy
pymysql
cryptography            # MySQL 8 认证插件需要

# 配置
python-dotenv
pydantic-settings

# 向量库 / Embedding
chromadb
jieba                   # 中文分词，BM25 关键词召回（001 混合检索；打分自研 Okapi BM25）
# 可选：Reranker 精排（001），安装见 docs/重难点总结.md 第五部分 §5 镜像教程
# sentence-transformers
# torch

# 文档解析
pypdf                   # pdf 解析（可选）
python-docx

# 鉴权
pyjwt
bcrypt                  # 密码哈希（passlib 与 bcrypt≥4.1 不兼容，直接用 bcrypt，见重难点总结 §1）

# 意图识别（006）：关键词多模式串匹配
pyahocorasick           # ahocorasick-ng 无 3.14/Windows 轮子，改用同 API 的上游包 2.3.1（见 docs/安装教程.md §1）
pyparsing               # 006 句式模板匹配（pyparsing.Regex 命名分组）
pyyaml                  # 006 词库/模板/负样本 YAML 配置

# LLM 调用（OpenAI 兼容协议，httpx 直连；可对接 Ollama / DeepSeek / 通义千问）
httpx

# 异步任务（002：002 已改为 FastAPI BackgroundTasks 进程内执行，见 specs/002-knowledge-base/research.md §1）
# 以下 celery/redis 当前不再必需，仅未来扩展回跨进程 worker 时才需要，故注释保留。
# celery
# redis
