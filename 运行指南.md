# 运行指南

> 如何运行整个项目（前端 + 后端），含模型 / API 配置。
> API Key 通过后端环境变量（`backend/.env`）配置，真实密钥禁止提交仓库。模板见 `backend/.env.example`。

## 项目结构

```
Nexus-Support-Agent/
├── backend/          # FastAPI 后端（Python 3.14）
│   ├── app/          # 源码（api / core / db / services / rag / intent ...）
│   ├── 数据库初始化脚本/init.sql
│   ├── requirements.txt
│   └── .env.example → 复制为 .env 填真实值
└── frontend/         # React 18 + Vite + Ant Design 5 前端
    └── package.json  # npm run dev / build / test
```

## 前置依赖

| 依赖 | 版本要求 | 说明 |
| --- | --- | --- |
| Python | 3.14 | 后端（实测兼容清单见 `backend/requirements.txt`） |
| MySQL | 8.0 | 业务数据 + `system_config` 配置表 |
| Node.js | ≥ 18（建议 20+） | 前端构建（Vite 6） |

> 无需 Redis / Celery：002 知识库后台任务已改为 FastAPI `BackgroundTasks` 进程内执行。

## 后端启动

```bash
cd backend
.venv/Scripts/pip install -r requirements.txt          # 首次：安装依赖
copy .env.example .env                                 # Windows；Linux 用 cp，然后填真实值
.venv/Scripts/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000   # 生产/局域网
# 开发热重载：
.venv/Scripts/python -m uvicorn app.main:app --reload --port 8000
```

- 健康检查：<http://localhost:8000/healthz>（应返回 `{"status":"ok"}`）
- API 文档（Swagger UI）：<http://localhost:8000/docs>
- 首次启动会自动：建表（`create_all`）、按 `.env` 预置管理员账号、Reranker 预热、清扫僵尸解析任务。

## 前端启动

```bash
cd frontend
npm install          # 首次
npm run dev          # http://localhost:5173，vite 自动把 /api 代理到后端 8000
```

- 后端地址默认 `http://localhost:8000`，可改 `frontend/.env.development` 的 `VITE_API_BASE`。
- 生产构建：`npm run build`（tsc 类型检查 + vite build）+ `npm run preview`。

## 模型 / API 配置（backend/.env）

复制 `backend/.env.example` 为 `.env` 后，至少填写以下必填项：

| 配置项 | 说明 |
| --- | --- |
| `DATABASE_URL` | MySQL 连接串：`mysql+pymysql://<user>:<pass>@<host>:<port>/<db>?charset=utf8mb4` |
| `JWT_SECRET` | JWT 签名密钥，生产 ≥32 字节随机串（默认值仅开发用） |
| `DEEPSEEK_API_KEY` | DeepSeek 官方 API 密钥（001 对话 + 006 意图兜底共用） |
| `EMBEDDING_API_KEY` | SiliconFlow API 密钥（Embedding，默认走云端 bge-m3） |

**LLM**：默认 `LLM_BACKEND=deepseek` 走 DeepSeek 官方 OpenAI 兼容 API（`LLM_MODEL=deepseek-v4-flash`，`LLM_BASE_URL=https://api.deepseek.com`）。如需本地 LLM，改 `LLM_BACKEND=ollama` + `OLLAMA_BASE_URL` / `OLLAMA_CHAT_MODEL`。

**Embedding**：默认 `EMBEDDING_BACKEND=openai_compat` 走 SiliconFlow 云端 bge-m3（`EMBEDDING_API_MODEL=BAAI/bge-m3`）。可选 `local`（本地 sentence-transformers）或 `ollama`（本机 Ollama 提供 bge-m3）。

**Reranker 精排（可选）**：混合检索主链路（向量 + BM25 + RRF 融合）开箱即用，无需精排模型；未安装 `sentence-transformers` 时自动降级为 NoopReranker，不影响主链路。如需精排（`bge-reranker-v2-m3` Cross-Encoder），安装见 [backend/README.md](backend/README.md)「API Key / 模型配置方式」，`.env` 置 `RAG_RERANKER_ENABLED=true`、`RAG_RERANKER_MODEL=BAAI/bge-reranker-v2-m3`。启动日志出现一次 `warmup_reranker` 成功即已生效。

**意图识别（006）**：`INTENT_ENABLED=true` 开启；小模型层可用独立 `SMALL_MODEL_*`（可指向第三方 OpenAI 兼容端点），三项均空时直接流转大模型兜底（复用 DeepSeek 配置）。

**链路埋点（008）**：`TRACE_ENABLED` / `TRACE_FLUSH_ENABLED` 总开关，后台批量落库，控制台打印完整链路块，`/api/trace` 可查。

完整配置项说明见 `.env.example` 注释与 `backend/app/config.py`。

## 数据库初始化

1. 按 `backend/数据库初始化脚本/init.sql` 在 MySQL 8.0 建库建表（业务表 + `system_config` 预置配置）。
2. 业务表也可由后端启动时自动创建（`create_all`）；`system_config` 默认配置仍需 SQL 预置。
3. 管理员账号不写死在 SQL：启动时按 `.env` 的 `ADMIN_ACCOUNT` / `ADMIN_PASSWORD` 自动预置 admin 角色，生产请修改默认密码。

## 测试

```bash
# 后端（backend 目录）
.venv/Scripts/python -m pytest tests/unit          # 单元测试（200 用例，无需外部依赖）
.venv/Scripts/python -m pytest tests/integration   # 集成测试（86 用例，需真实 MySQL，可选 LLM/Embedding）

# 前端（frontend 目录）
npm run test:unit    # Vitest 单元测试（39 用例）
npm run test:e2e     # Playwright E2E（直连真实后端，需先启动后端监听 8000）
```

## 常见问题

- **Embedding 初始化失败**：确认 `EMBEDDING_API_KEY` 有效、`EMBEDDING_API_BASE_URL` 可访问；`EMBEDDING_BACKEND` 与安装情况匹配。
- **Reranker 未生效**：未装 `sentence-transformers` 属预期降级；启动日志应只出现一次 warning。
- **文档上传后一直「处理中」**：后台解析为进程内 BackgroundTasks，超时（`PARSE_TIMEOUT_SECONDS`，默认 600s）自动置失败；重启服务会清扫僵尸任务。
- **前端 401 跳登录**：登录态存 `sessionStorage`，后端重启后旧 token 失效属正常，重新登录即可。
