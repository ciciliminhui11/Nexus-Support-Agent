# 运行指南

> API Key 通过环境变量（`.env`）配置，真实密钥禁止提交仓库。模板见 `backend/.env.example`。

## 后端启动

```bash
cd backend
.venv/Scripts/pip install -r requirements.txt          # 首次
copy .env.example .env                                 # Windows；Linux 用 cp，然后填真实值
.venv/Scripts/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

**依赖说明**：
- 必装：fastapi、uvicorn、sqlalchemy、pymysql、chromadb、sentence-transformers（bge-m3 Embedding）、jieba（BM25 中文分词）、httpx、pydantic-settings、python-dotenv。
- 数据库初始化：`数据库初始化脚本/init.sql` 建表并预置 `system_config` 默认配置（含 RAG 检索参数）。

## 前端启动

前端当前为骨架阶段（`frontend/package.json` 尚未配置 scripts）。规划：Vite + npm，`npm run dev` 开发模式，Vite 代理 `/api` → 后端 `http://localhost:8000`。骨架完成后按 README 补充。验证后端可直接用 curl 请求 `/api/chat/stream`（见 specs/001-rag-qa/quickstart.md）。

## Reranker 精排模型（可选）

混合检索主链路（向量 + BM25 + RRF 融合）开箱即用，**无需**精排模型；未安装时自动回退 RRF 融合序，不影响验收主链路。

如需精排（FR-014，`bge-reranker-v2-m3` Cross-Encoder），按 **docs/重难点总结.md 第五部分 §5** 的镜像安装教程执行：清华/阿里 PyPI 镜像装 sentence-transformers + torch CPU 版，HF 权重走 hf-mirror.com 或 ModelScope 下载，`.env` 设 `RAG_RERANKER_MODEL`（本地路径或模型名）。

```bash
# 已安装后置 env（Windows PowerShell 用 $env:）
export RAG_RERANKER_MODEL=BAAI/bge-reranker-v2-m3
export RAG_RERANKER_ENABLED=true
```

启动日志应出现一次 `warmup_reranker` 成功；未安装时日志出现一次降级 warning 且链路正常。

## 数据库初始化

1. 按 `数据库初始化脚本/init.sql` 在 MySQL 8.0 建库建表（业务表 + `system_config` 预置配置）。
2. 管理员账号由应用启动时按 `.env` 的 `ADMIN_ACCOUNT`/`ADMIN_PASSWORD` 自动预置，不写死在 SQL。
3. 模型：Embedding（bge-m3）与 LLM（Ollama Qwen2）本地启动，base_url / 模型名在 `.env` 配置。
